/**
 * Created by Jason on 15/6/4.
 */

/**
 *  废弃组件
 */
function classline() {
    /*var num = 0;
    num = $("div .hex-class-line").children("div").size() - 2;

    if(num==1){
        $("a[hex-role='left']").parent().css("display", "none");
        $("a[hex-role='right']").parent().css("display", "none");
    }

    var div_content = $("div[class='hex-little-btn']").next();

    for (var i = 1; i < num; i++) {
        div_content.next().css("display", "none");
    }

    $("a[hex-role='left']").click(function show() {
        showleft_class(this);
    });

    $("a[hex-role='right']").click(function show() {
        showright_class(this);
    });

    function showleft_class(thisObj) {
        var first = $("div .class:not([style='display:none;'])");
        //隐藏已有的
        first.css("display", "none");

        //渐进出现新的
        first.prev().fadeIn(500);
    }

    function showright_class(thisObj) {
        var first = $("div .class:not([style='display:none;'])");
        //隐藏已有的
        first.css("display", "none");

        //渐进出现新的
        first.next().fadeIn(500);

    }*/
}