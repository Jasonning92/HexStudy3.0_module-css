/**
 * Created by Jason on 15/6/1.
 */
function littlebooklist() {
    //var length = document.documentElement.clientWidth - 80 - 120 - 170;
    /*var length = $("div .hex-little-book-list").parent().width();

    var oneclass = $("div .hex-little-book-list").width();
    var num = Math.floor(length / oneclass);//一行可以放多少个
    var object = $("div .hex-little-book-list").parent().children().first();
    var classnum = $("div .hex-little-book-list").parent().children().size();
    for (var i = 0; i < classnum; i++) {
        if (i % num == 0) {
            object.css("margin-left", "0px");
        }
        object = object.next();
    }*/
}